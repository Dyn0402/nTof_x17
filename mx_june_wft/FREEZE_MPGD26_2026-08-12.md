# FREEZE_MPGD26_2026-08-12 — the frozen analysis and the full-June condor campaign

**The micro-TPC waveform-first reconstruction is frozen for MPGD26 at the
commit recorded in `condor_campaign/FREEZE_COMMIT.txt` (built by
`condor/make_reco_package.py`, which refuses a dirty tree). The full June
cosmic dataset — 214 (subrun, detector) rows across 144 subruns — is
enumerated, classified against the frozen calibrations, and packaged for
lxplus condor. This file is the campaign runbook.**

Development continues after the conference; the open items are in §8. Nothing
in this campaign changes reconstruction behaviour — it is the frozen chain at
scale, plus one *gated* config decision (the t0 prior, §4).

## 1 · What is frozen

| piece | state |
|---|---|
| `wft/` code | HEAD (multi-track merge `39db695`; single-track output verified bit-identical). Tests: `wft/tests/` 9/9 pass. |
| det3 bundle | `calib_bundle_lp2_t0p` = lp2 hypers + t0_abs + σ=5 trigger prior (validated, `T0_PRIOR_2026-08-11.md` §8) |
| det2/4/6/7 bundles | `calib_bundle_lp` (each detector's own share-lp-generation fit — what the 7-31 fleet recos used; det6 upgraded from its legacy-kernel bundle, det7's older `calib_bundle_v36` superseded 7-31) |
| accounting | `02_efficiency.py` JSON now carries `eff_vs_R` + |r| histogram; `mpgd26/make_efficiency_breakdown.py` builds conference figures from that file alone (commit `12c437a`) |
| judged & rejected 8-11/8-12 | kTauY naive port, tauy 2-param refit, cx0, p0-shear (any strength), full recalibration — all bench-judged, all trades or regressions (`KERNEL_ARMS_2026-08-12.md`). The freeze inherits NONE of them. |

## 2 · ⚠️ Discovery made while freezing: production runs the `delay` kernel branch

`wft/model.py` selects the sharing-kernel form from the bundle's `share_mode`
field. **Every production bundle carries `share_mode: null`** → the loader
falls back to `'delay'` (delayed smeared-template copies), not the `'lp'`
cascaded-one-pole branch the "share_lp" name suggests. The `share_lp: 1.0`
entry in `hyper` is inert — nothing reads it. Nothing in the analysis tree
calls `set_share_mode`, so **the bench harness scored the same branch**:
every number since the 8-06 merge — the fleet digest, the t0-prior A/B, the
kernel arms' "shipped behaviour" reference, the multi-track bit-identity — is
the delay branch with the lp-fitted hypers. The system is *self-consistent*;
only the naming ("share_lp kernel") is misleading.

Measured (sat_det3, 250 events, lp2_t0p, stamp `share_mode='lp'` vs as-is):
median |Δp0| 10–20 µm, 5–8 % of events hop > 0.5 mm (candidate/basin flips),
lp chi2 ~4 % lower, quality_ok +0.4/+4.0 pp (x/y). Neither branch is
"validated better" — the *delay-as-loaded* behaviour is what every gate
measured, so **the freeze keeps it**. Stamping `'lp'` (or deciding between the
branches properly) is a post-MPGD26 item together with the Y kernel shape
(F6/F12); it would re-open every gate.

## 3 · The manifest — what runs and what is held

`condor/make_manifest.py` walks the local bench mirror (ground truth for the
7-24 reprocessing), reads each run's `run_config.json` (detector→FEU map,
per-subrun resist/drift HV, gas) and classifies each row against the frozen
bundle's own conditions (read from the golden runs' configs, never hardcoded).
Output `condor/campaign_manifest.csv` (+ `condor/eos_index.json`, which maps
every run to its EOS dir — incl. the two runs whose `june_tests/` copies are
the STALE duplicates; the index points at `det_3_old/` for those).

| tier | rows | meaning | treatment |
|---|---|---|---|
| A | 22 | golden conditions (same resist, drift, gas as the bundle) | direct frozen-bundle reco |
| B | 7 | det3 drift scan (same resist, other drift V) | per-subrun v_drift refit (`wft.calibrate --fix-hyper` with all 7 kernel hypers pinned, `--seed-bundle` frozen) then reco; refit bundle ships with the output |
| C (resist scans) | 130 | HV-scan points at non-bundle resist V | reco with the detector's frozen bundle, output stamped `off_conditions: true` — **QA/trend grade** (efficiency-vs-HV shapes), never absolute geometry. A bundle outside its conditions is a silent error (CLAUDE.md); the stamp makes it a loud one. |
| C (excluded) | 55 | pre-June (37), det1 = no wft calibration ever (part of 49 det1 rows), no run_config (5), stale duplicate trees, gas mismatch (ArIso 6-16) | not run; each row carries its reason so nothing drops silently |

All four golden datasets (det6+det7 share one) are on EOS under
`/eos/experiment/ntof/data/x17/cosmic_bench/june_tests/` **with
`decoded_root`** — nothing needs uploading.

## 4 · The t0-prior gate (decided policy: calibrate + gate)

The σ=5 trigger prior is validated **on det3 only**. For det2/4/6/7:

1. ftst-carrying bench caches rebuilt (`bench_cache_ftst.pkl`, 8-12 — the
   7-30 caches lack per-plane `ftst`).
2. `08_t0_abs_calib.py <key> --bundle .../calib_bundle_lp` per detector →
   `wft/t0_prior/t0_abs.json`. (det2's ftst ladder is visibly noisier than
   det3's clean −10 ns/step — expect the gate to actually decide something.)
3. Gate bundles `calib_bundle_lp_t0p` = copy of `calib_bundle_lp` +
   `t0_abs` + `t0_prior_sigma: 5.0` (same construction as det3's, T0_PRIOR §8).
4. **Phase-1 condor jobs** run each golden key twice: `prod` (frozen bundle,
   = the 7-31 configuration re-run on frozen HEAD) and `t0p`. Full statistics,
   production code path.
5. `condor/gate_eval.py <key>` scores both arms through the standard
   01→02→03 accounting (backup-swaps the live parquet, restores even on
   error) and prints the verdict. **Rule: ADOPT iff no regression beyond
   (within5 −0.1, far +0.1, core +2 %) and ≥1 improvement.** det3 is not
   gated (already validated). ~~its prod arm doubles as the non-regression
   check against `FLEET_DIGEST.md`~~ — **struck 8-12, both premises false;
   see §7**: the digest predates the current code generation and det3's
   manifest bundle already carries the prior. There is no valid external
   reference for the campaign; its internal A/Bs and the regenerated digest
   are the record.
6. Adopted detectors get `--t0p-dets mx17_X,…` at the phase-2 package build;
   fallback detectors run `calib_bundle_lp` everywhere. Mixed fleet config is
   acceptable and is recorded per row in `job_row.json`.

## 5 · Condor campaign — phases (the opus session's runbook)

Package: `condor/make_reco_package.py` → `/home/dylan/x17/cosmic_bench/condor_campaign/`
(code.tar.gz at the freeze commit — the script refuses a dirty tree —
bundles.tar.gz incl. gate bundles, manifest, `jobs_gate.txt` /
`jobs_rest.txt`, scripts). One job = one manifest row: fetch that subrun's
det-FEU `decoded_root` + `combined_hits_root` + `m3_tracking_root_v2` +
`run_config.json` from EOS (xrdcp, fuse fallback; `MY.SendCredential`),
synthesize the `_Config`, reco with `--matched-only`, tar back
`events(.candidates).parquet` + meta + `job_row.json`.

```
# 0. prerequisites (local)                     [DONE unless noted]
mx_june_wft/condor/make_manifest.py --eos-index mx_june_wft/condor/eos_index.json
mx_june_wft/condor/make_reco_package.py        # rebuild AFTER t0p bundles exist

# 1. ship + phase 1 (gate: 5 prod + 4 t0p arms, ~1-2 h wall)
kinit <user>@CERN.CH
rsync -av /home/dylan/x17/cosmic_bench/condor_campaign/ lxplus:~/wft_campaign/
ssh lxplus 'cd ~/wft_campaign && mkdir -p log && condor_submit reco.sub'

# 2. gate (local)
rsync -av 'lxplus:~/wft_campaign/reco_r*.tar.gz' /home/dylan/x17/cosmic_bench/condor_campaign/back/
mx_june_wft/condor/collect_results.py
mx_june_wft/condor/gate_eval.py o22_long_det2   # ×4 keys — record verdicts
                                                 # in this file, §7

# 3. phase 2 (the other ~154 jobs)
mx_june_wft/condor/make_reco_package.py --t0p-dets <adopted, maybe none>
rsync -av /home/dylan/x17/cosmic_bench/condor_campaign/ lxplus:~/wft_campaign/
ssh lxplus 'cd ~/wft_campaign && condor_submit jobfile=jobs_rest.txt reco.sub'

# 4. collect + promote (prod rows only; existing outputs parked in
#    wft/pre_campaign_backup/; offcond stays in staging with its flag)
mx_june_wft/condor/collect_results.py --promote

# 5. downstream, per golden key then per tier-A key (local, minutes each):
#    01_alignment → 02_efficiency (both variants + hits comparison) →
#    03_angles → 04_maps → digest  (= run_chain.sh --skip-reco)
bash mx_june_wft/run_chain.sh <key> --skip-reco
.venv/bin/python mx_june_wft/digest.py sat_det3 o22_long_det2 g_det4 g_det6_long g_det7_long --out mx_june_wft/FLEET_DIGEST.md

# 6. conference outputs
.venv/bin/python mpgd26/make_efficiency_breakdown.py     # reads 02's JSONs
#    then mpgd26/make_report.py + the slide regen per mpgd26/README.md
```

Condor shape: `request_cpus=8`, 6 GB, `workday` flavour, `max_retries=2`,
hold on nonzero exit. Environment LCG_105 (numpy/scipy/pandas/uproot/pyarrow
— same view as the July beam processing). Biggest single job ≈ det4 golden
(12 k events, ~3.5 core-h → ~30 min at 8 cores + fetch). Whole campaign
≈ 60–100 core-h. Condor gotchas from the July campaigns apply
([[mx17-condor-liveness-signals]]): don't trust `qedit`, empty `condor_q` ≠
done — count result tarballs.

Local smoke of the full job machinery (row 41 = det3 in the 6-22 overnight
run, tier A non-golden, `--limit 25`): PASSED 8-12, candidates sidecar and
`job_row.json` present.

## 6 · Rules that bind the campaign

- **Never merge or compare results across bundles** without saying so — a
  fit is labelled by its bundle (`job_row.json.bundle_used`); off-conditions
  rows carry `off_conditions: true` and are quoted as trends only.
- **Promotion is explicit** (`collect_results.py --promote`), prod rows only,
  never gate arms; anything replaced is parked in `wft/pre_campaign_backup/`.
- Status comes from the JSON summaries, not from prose in logs
  ([[analysis-status-from-summaries-not-logs]]). ~~validate det3 against the
  known digest row~~ — struck 8-12 (§7): `FLEET_DIGEST.md` is a historical
  artefact of the pre-freeze code and is not a validation reference. Sanity
  checks are internal: arm-vs-arm, and campaign-digest implied-v flatness.
- The t0p arms must NOT be produced with CLI flags — the prior lives in the
  bundle, and `t0_prior_for` activates it from bundle fields alone (verified
  in the 8-12 smoke).

## 7 · Gate verdicts (fill in during the campaign)

Gate phase = condor cluster 3924896 (schedd `bigbird101.cern.ch`), 9 jobs,
frozen package at `effef73`. Scored with `condor/gate_eval.py` (01→02→03,
`--max-dropped -1`, i.e. the headline JSON the digest reads).

| det | prod within5/far/core | t0p within5/far/core | verdict |
|---|---|---|---|
| mx17_2 | 91.687 / 4.591 / 0.449 | 91.979 / 4.296 / 0.426 | **ADOPT** — within5 +0.29, far −0.30, core −5.1 %, median −0.029, σθY −0.034, vsp_x −0.375; only vsp_y up (+0.169, not in the rule) |
| mx17_4 | 41.666 / 7.653 / 0.702 | 41.589 / 7.717 / 0.561 | **ADOPT** — the strongest arm: core −20 %, median −16 %, σθ −0.25/−0.29°, vsp better on both planes; within5 −0.077 / far +0.064 stay inside tolerance |
| mx17_6 | 74.938 / 3.895 / 0.461 | 74.811 / 4.018 / 0.461 | **FALLBACK** — marginal: within5 −0.127 and far +0.123 each just past the gate; core flat, median −0.026 |
| mx17_7 | 56.936 / 9.168 / 0.620 | 56.577 / 9.641 / 0.573 | **FALLBACK** — within5 −0.359, far +0.474; a real trade, since core −7.6 %, median −4.6 % and every angle metric improves |

### ⚠ The §4/§6 non-regression check against `FLEET_DIGEST.md` does not work

Both halves of it fail, so no campaign result should be checked against that
file. Measured 2026-08-12 while running the gate:

1. **det3's arm and the digest row are different bundles.** The live parquet
   the digest was built from carries `calib_bundle_lp2`, `t0_prior_sigma:
   None`, `t0_abs: {}` — no prior — while the campaign's det3 prod arm is
   `calib_bundle_lp2_t0p`. The comparison measures the prior, not a regression
   (campaign 93.276 / 4.015 / 0.447 vs digest 93.5 / 3.7 / 0.46).
2. **The frozen code does not reproduce the 7-31 recos even at identical
   bundle.** det2 prod vs the live 7-31 parquet — same `calib_bundle_lp`, same
   3666 events, same `event_id`s — differs in 39 of 45 shared columns: median
   |Δp0| 0.161 mm (21 % of events move > 0.5 mm), median |Δθ| 0.46°, median
   |Δt0| 41 ns, `x_quality_ok` 0.990 → 0.603. `wft/` has had the RC-ladder
   kernel + per-plane angle constants (`9dd7d6e`), the t0 prior (`f9e18d2`) and
   multi-track (`39db695`) since, so this is expected code evolution — but it
   means FLEET_DIGEST is an artefact of older code and is not a reference.

Aggregate impact is small (det2 prod within5 91.687 vs 91.9, far 4.591 vs 4.4)
**except σθX, 1.14 → 1.30 (+14 %)** — check that before any slide quotes
angles. `quality_ok` does not gate the headline accounting (`compat.load_table`
applies it only under `quality_only=True`), so the collapse does not touch
within5/core. Gate A/Bs are unaffected: both arms come from the same frozen
code. The digest must be **regenerated from campaign outputs** (§5 step 5),
not compared against.

Provenance gap: the bundles record `code_commit: 1292d91`, which is not a valid
object in this repo, so the 7-31 behaviour cannot be bisected as things stand.
(This matches the recorded trap in [[wft-t0-prior-gate]]: the validated
R&D-line config "is NOT in shipped code; events.parquet (commit 1292d91) is
not reproducible by it" — known since 8-11, under-weighted when §4 was
written.)

**σθX resolved to a population/mapping effect, not degraded fits (8-12).**
On det2 events `slope_reliable` in BOTH generations, median |Δw| is
0.0002–0.0004 (≈ 0.01–0.02°) — the per-event slopes are unchanged. What moved:
`slope_reliable` 0.663 → 0.620 (x) / 0.610 → 0.535 (y), i.e. the σθ
*population* changed, and `9dd7d6e`'s per-plane angle constants changed the
w→angle map. The trustworthy judge (implied-v flatness, WFT §35) improves
dramatically under the frozen code (det3 vsp 2.12/3.40 → 0.78/1.15). Slides
must quote angle numbers ONLY from the regenerated campaign digest — the old
σθ 1.08/1.11 det3 figures in `mpgd26/slides/NOTES.md` are pre-freeze vintage.

### Tier B (det3 drift scan): DEFERRED from the conference path (8-12)

All 7 `--vrefit` jobs fail by design gap: `wft.calibrate` needs hits-chain
reference products (`cache/event_results_veto50.pkl` +
`alignment_tpc_veto50/alignment.json` from `03_alignment_and_tpc.py --veto=50`)
that were never produced for the drift-scan subruns and that no condor job
stages. Decision: **defer** — the drift-gap physics for the conference is
already covered by the 7-30 bench-line results (`GAP_STUDY_2026-07-30.md`,
`gap_vs_drift`), which remain valid; no slide needs a fresh wft reco of the
drift scan. To unblock post-conference: run `03_alignment_and_tpc.py <key>
--veto=50` locally for the six drift-scan subruns (row 100 also lacks the
event cache), then either run the v-refits locally or extend the job to ship
the alignment products.

### Promotion nuance for ADOPTED detectors (design gap, resolved 8-12)

The golden rows of adopted detectors (det2 row 22, det4 row 118) ran only in
the gate phase, where the prior arm is the *tagged* `<key>__t0p` staging dir —
and `collect_results.py` refuses tagged arms by design. A plain `--promote`
would therefore install their NO-prior arms while every other det2/det4 row
carries the prior. Resolution (bookkeeping, no re-run — the gate arm IS the
production artifact: same row, same frozen code, same verified bundle, full
statistics): park the no-prior arm as `<key>__prod_noprior` with its
`job_row.json` untouched (the gate record), move the t0p arm into the
promotable `<key>` slot with `job_row.json` corrected to
`bundle_used: calib_bundle_lp_t0p`, tag cleared, and a provenance note naming
the gate cluster it came from; then promote through the normal path. Final
fleet config the digest describes: det2 `lp_t0p`, det3 `lp2_t0p`, det4
`lp_t0p`, det6 `lp`, det7 `lp`.

### ⚠ POSTMORTEM: the 7-31 golden parquets were destroyed during promotion (8-12)

Two compounding faults, one from each session. (1) `collect_results.py`
skipped promotion when `'__'` appeared *anywhere* in the staged name — meant
for `<key>__t0p` arms, but every synthesized tier-A key is
`c26__<run>__…`, so the first `--promote` silently promoted only the 5 golden
keys, looking successful. (2) The re-run after diagnosing this was not
idempotent: promote unconditionally parked "whatever is live" into
`pre_campaign_backup/`, and on the second pass the live file was the pass-1
campaign output — which landed on top of the 7-31 original.

**Lost:** the exact 7-30/7-31 `events.parquet` for **four** golden runs:
det2/det3/det4/det7. **det6 was RECOVERED** (8-12, logistics session):
`prev_20260731_prelp` proved to *be* the lost content — byte size recorded
before the overwrite (2116617), md5-identical to `prev_20260730_oldkernel`
(det6 was never re-reco'd through the share-lp generation, so all three
copies were the same file), and it reproduces the pre-campaign digest's det6
column to printed precision through the standard accounting; restored into
`pre_campaign_backup/` with `RECOVERY_NOTE.md`. For the other four, recovery
was searched and exhausted: `~/x17` is a symlink to
`/media/dylan/data/x17` (one tree, no second local copy), the desktop mirror
has only an 8-05 det4 vintage, EOS has no wft parquets, `gate_backup/` dirs
restored clean (empty), and the `prev_*` dirs are *predecessor* generations
parked 30–40 min before the lost recos, not the lost generation. Not
regenerable: the producing code (`1292d91`) is not in this repo.

**Survives:** all campaign outputs; every tier-A backup (promoted exactly
once); the 7-31 aggregates as text in the pre-campaign `FLEET_DIGEST.md`
revision (and in git history); the per-event det2 old-vs-new comparison in
this file; the `prev_*` predecessor generations. Since `FLEET_DIGEST.md` had
already been demoted from reference status (see above), the scientific cost
is confined to per-event forensics on a superseded generation.

**On-disk record corrected:** each golden `pre_campaign_backup/` now carries
`LOSS_NOTE.md` stating that its contents are campaign pass-1 files, not
pre-campaign state — the name is wrong and cannot be trusted there.

**Fixes (committed):** promotion now matches the staged dir name against
`row['key']` *exactly* (skips tagged arms, includes `c26__` keys — note that
filtering on `out_tag` instead would promote the parked no-prior arms), and
an existing `pre_campaign_backup` entry is never overwritten — the first
backup is the true pre-campaign state; a later pass removes the live file
instead of parking it. Lesson, stated plainly: **any step that moves
irreplaceable files must be idempotent before it is run twice, and "looked
successful" is not a check** — the first promote printed 5 promotions and no
warning about the 17 skips.

### Campaign digest regenerated (8-12) — the conference numbers

`FLEET_DIGEST.md` now describes the campaign fleet config (det2 `lp_t0p` /
det3 `lp2_t0p` / det4 `lp_t0p` / det6 `lp` / det7 `lp`), and every column
cross-checks against its adopted gate arm to rounding. det3 gate: all
thresholds met. Headlines vs the (dead) pre-campaign digest: det4 improves
most (core 0.67→0.56, σθY 2.86→2.45), det7 σθY 2.09→1.75, det3 within5
93.5→93.3 / core 0.46→0.45. **Every σθX is higher than the pre-freeze
figures quoted in old slides (det3 1.08→1.16)** — this is the
slope_reliable-composition + w→angle-mapping effect (see above), not fit
degradation, and any figure caption quoting angles must say so.

Chain caveat for future reruns: `run_chain.sh`'s first stage rebuilds a
bundle from legacy `waveform_first/` artifacts and FAILS for det2/det4
(missing `templates_perplane.npz`/`hyper_v2.json`). The accounting stages
(01/02/03/04) were run directly for det2/4/6/7 — identical to what run_chain
runs after that stage, and no accounting stage consumes the rebuilt bundle
(03 reads v from `events.meta.json`). det3 ran through run_chain intact.

### Off-conditions rows: v1-only M3 tracking

All 130 off-conditions rows have no `m3_tracking_root_v2` — their M3 recipe is
`[chi2<1]` with no `NClus>=4` clause, unlike the golden rows. A second,
independent reason they are trend-grade only.

### Known data gaps (rows removed/held, none a code fault)

- Rows 90/91 (`resist_465V_drift_600V`, det3+det4): telescope not recording
  (v1 file with zero tracks); frozen reco raises `KeyError: 'event_id'` on an
  empty match list instead of writing an empty table — accepted, not patched
  mid-campaign.
- Row 202 (`det3_test_6-22-26/short_run`): stale pre-7-25 hits (no
  `significance` branch); full classification of held jobs at campaign end.

## 8 · Explicitly NOT in this campaign (post-MPGD26 queue)

- **Restore the w0/kw angle-constant application in `plane_fit`** —
  `f9e18d2` silently reverted `9dd7d6e`'s
  `tan = (w·1e3 − w0)/(kw·v)` to `tan = w·1e3/v`; every bundle carries the
  calibrated constants unapplied, which is the fleet-wide Y-heavy angle
  bias in the campaign digest (det6 Y −1.04° ≈ arctan(w0_y/v) = −1.14°).
  Corrected numbers via `quality_investigation/corrected_angles.py` →
  `wft/angles_w0corr/`; full audit in
  `quality_investigation/INVESTIGATION_2026-08-12.md`.
- **Make `quality_ok` amplitude-aware** — the absolute χ²/dof<300 threshold
  on a pedestal-weighted χ² is an amplitude cut (exponent 2.0 measured);
  use the gain-normalized shape χ² instead. Also ~2 % NNLS q_sum blow-ups
  (>1e6) to guard.

- `share_mode` stamping / delay-vs-lp adjudication (§2) and the Y kernel
  *shape* (F6/F12) — the real kernel project.
- Merged-cluster doubles (< 12 mm) — tier-3 multi-track
  (`wft/MULTITRACK_2026-08-12.md`).
- det1: no wft calibration exists; its 49 rows are excluded, not forgotten.
- Pre-June data (Jan–May): different DAQ/CNS epochs, own campaign if ever.
- Any recalibration — both 8-12 recal basins were trades; the freeze keeps
  lp2/lp bundles as they are.

## 9 · Campaign execution record (closed 2026-08-12)

Clusters: gate `3924896` (9 jobs), phase 2 `3924904` (154 submitted, 7 removed).
Both on schedd `bigbird101.cern.ch` — **query condor by name**; `ssh lxplus`
round-robins login nodes bound to different schedds, and an unpinned
`condor_q` returned an empty list for jobs that were running fine (the tell is
the cluster-ID range and pool total changing between polls).

```
manifest rows                            214
  runnable                               159
  excluded by manifest                    55   37 pre-June, 12 no frozen bundle,
                                               5 no run_config, 1 no decoded files
runnable outcome
  produced a result                      149
  tier B removed (alignment prereq)        7   rows 61-66, 100
  terminal data failures                   3   rows 90, 91, 202
  unaccounted                              0   nothing dropped silently

  tier A on-condition (absolute geometry)  22   det2 4, det3 4, det4 5, det6 4, det7 5
  tier C off-conditions (TREND-GRADE)     127   det2 25, det3 50, det4 11, det6 21, det7 20
promoted into the Analysis tree            22   5 golden + 17 tier-A non-golden
```

Indexing caveat: the manifest's 214 rows carry only 210 unique `key` values —
the 5 no-run_config rows share the empty key (and golden registry keys repeat
nowhere else). Any key→row mapping must fall back to the row index;
`run_reco_job.py` always addresses by row index, so the campaign was immune.

Off-conditions rows are staged only, never promoted, and carry
`off_conditions: true`. **Second reason they are trend-grade:** all 130 have
only `m3_tracking_root` v1, no v2 — their M3 recipe is `[chi2<1]` with no
`NClus>=4` clause, unlike the golden rows' `[chi2<1 & NClus>=4]`. The reference
cut genuinely differs between golden and HV-scan rows.

### Terminal failures — data, not code

| row | detector / subrun | cause |
|---|---|---|
| 90 | mx17_3 · det3_det4 6-23 / `resist_465V_drift_600V` | 0 M3 tracks — telescope not recording (0.36 MB v1 file). The chamber gave 1 772 seeded events. |
| 91 | mx17_4 · same subrun | same, 3 797 seeded events |
| 202 | mx17_3 · det3_test 6-22 / `short_run` | `combined_hits` predates the 2026-07-25 significance floor — no `significance` branch |

Rows 90/91 expose a frozen-code edge: an empty reconstruction raises
`KeyError: 'event_id'` at `reco.py:575` (`pd.DataFrame([])`) instead of writing
an empty table. Left alone — not worth touching frozen code mid-campaign.

One transient failure recovered: row 150 died on `OSError: Errno 5` mid-fetch
and succeeded on release. **`max_retries = 2` in `reco.sub` never fires** —
`on_exit_hold` takes precedence, so nothing auto-retries and every transient
failure waits for a human. Fix that before the next campaign.

### Verification that the promoted fleet is coherent

Each golden key's live `events.parquet` is byte-identical (md5) to its staged
campaign result, and its live `efficiency_breakdown.json` matches the
regenerated digest: det3 93.276/0.447, det2 91.979/0.426, det4 41.589/0.561,
det6 74.938/0.461, det7 56.936/0.620.

⚠ `gate_eval.py` restores parquets but **not** the accounting JSONs it
produces, so after any arm evaluation the live `efficiency/`, `angles/` and
`alignment/` describe the last arm evaluated, not the live parquet. This
mismatched det6's figures by 0.5 pp on 8-12. Always re-run 01→04 on the live
parquet afterwards; the docstring now says so.

### Loss and recovery

det6's pre-campaign parquet was **recovered** and restored (see that
directory's `RECOVERY_NOTE.md`): `prev_20260731_prelp/events.parquet` matches
the lost file's byte size, is md5-identical to `prev_20260730_oldkernel`, and
reproduces the pre-campaign digest's det6 column to printed precision. Net
loss is four keys — det2, det3, det4, det7 — as recorded in §7.
