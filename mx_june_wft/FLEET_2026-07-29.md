# The June fleet on the waveform-first reconstruction

**2026-07-29.** Every golden key reconstructed with `wft` and analysed with
`mx_june_wft`. Method and caveats: `HANDOFF_2026-07-29.md`; the det3 gate that
preceded this: `DET3_GATE_2026-07-29.md`; why the rebuild exists at all:
`../RECONSTRUCTION_BASIS.md`.

## Position

Both chains through the same accounting, and — as of 04:48 — **all five hits
caches now carry the 2026-07-25 significance floor**, so this table is finally
apples to apples with no caveats:

| detector | hits chain (floor-corrected) | **waveform-first** | Δ | core σ hits → **wft** |
|---|---|---|---|---|
| det3 | 93.13 % | **93.47 %** | +0.34 | 0.45 → 0.47 mm |
| det2 | 92.06 % | **92.07 %** | +0.01 | **0.43** → 0.48 mm |
| det4 | 40.67 % | **41.65 %** | +0.98 | **0.66** → 0.69 mm |
| det6 | 71.19 % | **75.41 %** | +4.22 | **0.38** → 0.43 mm |
| det7 | 52.73 % | 56.95 % | +4.22 | **0.52** → 0.63 mm |

(within 5 mm of the M3 reference.)

**This supersedes the +10 to +31 point gaps quoted earlier tonight.** Those came
from comparing against caches that predated the significance floor, and the fix
is worth a great deal on exactly the detectors it had never been applied to:
det2 80.86 → 92.06, det6 44.46 → 71.19, det7 18.10 → 52.73, with core σ
improving from 0.63/0.67/2.72 to 0.43/0.38/0.52 mm. That closes the July-25 open
item, and it lands almost exactly on the offline estimates made at the time
(det2 91.4 %, det6 70.9 %).

So on **position** the honest summary is: **parity to +4 points**, and the hits
chain now has the better core σ on det2, det4, det6 and det7. The waveform chain
wins on the fraction of tracks it places within 5 mm; the hits chain places its
good ones slightly more precisely. That is a much narrower claim than it looked
an hour ago, and it is the right one.

## Angles — the point of the exercise

| detector | σ_θ X / Y, hits | σ_θ X / Y, **wft** | bias X / Y | implied-v spread |
|---|---|---|---|---|
| det3 | 2.42 / 2.60° | **1.20 / 1.14°** | −0.04 / −0.29° | 2.3 / 2.4 µm/ns |
| det2 | 2.47 / 2.04° | **1.31 / 1.56°** | −0.38 / −0.38° | 3.9 / 5.3 |
| det6 | 3.42 / 2.58° | **2.28 / 2.52°** | +0.03 / −0.95° | 4.8 / 6.4 |
| det7 | 2.82 / 2.54° | **1.96 / 1.71°** | −0.10 / −0.31° | 1.3 / 2.7 |
| det4 | 3.38 / 3.18° (2.60 / 2.50 pre-regression) | 2.73 / 2.58° | −0.21 / −0.30° | 4.9 / 5.0 |

Roughly 2× better on det3, det2 and det7, and clearly better on det6; det4 only
matches its pre-regression numbers. Bias is consistent with zero everywhere
except det6-Y. The implied-v spread — the median `w / tan_ref` across
angle bins, which must be flat if the reconstruction is geometrically honest —
runs 1.3–6.4 µm/ns against the production ladder's ~17. That spread *is* the
compression signature, and it is largely gone.

## Two findings worth following up

**det6's X plane is not dead.** `JUNE_RESULTS_SUMMARY.md` records it as dead for
micro-TPC (v railing to 119 µm/ns, plateau σ68 ≈ 16°, board-C mesh defect). The
waveform fit gets σ_θ 2.28° with +0.03° bias from it over 4,892 events. A
velocity railing to 119 µm/ns is a degenerate fit, not a measurement. The plane
is degraded, not dead — see the handoff for the two checks that would settle it.

**det7's calibration is still unsolved — pinning v moved the problem, it did not
fix it.** The recalibration with `--fix-v 26.4` and the c1 floor produced a
physical-looking kernel (c1 = 0.340, kY = 2.03, τ_s = 62 ns, σ_s = 100 ns, in
family with det3/det2/det6) and the drift-scan velocity. But σ_p0 railed at its
lower guard (0.0301 against a bound of 0.03), and the angles got **worse**, not
better:

| det7 | degenerate calibration | recalibrated, v pinned |
|---|---|---|
| kernel | c1 = 0.004, kY = 6.6, σ_p0 = 0.52 mm | c1 = 0.340, kY = 2.03, σ_p0 = 0.030 mm (railed) |
| v | 36.68 µm/ns | 26.40 (pinned) |
| within 5 mm | 57.33 % | 56.95 % |
| σ_θ X / Y | 1.96 / 1.71° | **7.05 / 4.47°** |

So one calibration has an unphysical kernel and good-looking angles, the other a
physical kernel and bad angles — and neither should be quoted. **det7's angle
row in the table above is from the degenerate fit and is not trustworthy
either.** The likely root cause is the same thin-template problem as det6 (det7
is the other low-gain chamber); the fix is to re-fit the templates with loosened
cuts before touching the kernel again. Both bundles are kept side by side
(`calib_bundle` and `calib_bundle_degenerate_*`).

**The calibration's v is not identifiable from one detector alone.** det7's fit
ran to c1 = 0.004 (no charge sharing, impossible on resistive strips) with
v = 36.68 µm/ns at a field where the drift scan measures 26.4 — yet its angles
came out unbiased at 1.96 / 1.71°. The sharing kernel and v trade off while
`tan θ = w / v` stays determined (`WAVEFORM_FIRST_THREADING.md` §17.2). So a
fitted per-detector v is a nuisance parameter unless it is checked against an
independent measurement. det3's and det6's do agree with the drift scan (det6's
free fit landed 1.3 % from det3's measured 700 V value) — that agreement is what
makes them trustworthy. det7 is being recalibrated with v pinned.

## Reproduce

```bash
mx_june_wft/run_fleet.sh          # every key, sequential
mx_june_wft/digest.py sat_det3 o22_long_det2 g_det4 g_det6_long g_det7_long
```
