# run_145 re-reconstructed on the corrected sharing kernel

Record of the redo called for by `HANDOFF_RUN145_R06_2026-08-19.md`. Three of
the four arms were seeded from a bench bundle whose sharing ladder was inverted
(c₂ > c₁, which cannot happen — the ±2 strip is reached only through the ±1);
they now run on `calib_bundle_r06`.

**Scope.** run_145, sub-runs `stat090_0000` (7 file tags) and `stat090_0001`
(8), all four arms. Arm C is on `calib_bundle_lp` throughout — det6 was never
inverted, so C is **not** a correction; it is re-run only so that all four arms
sit on one code generation and one set of local tables.

---

## 1 · Two defects, not one

The handoff named one. The controlled comparison found a second.

**(a) The sharing kernel** — the one we set out to fix. A→det3 1.14, B→det2
1.53, D→det7 1.75, all now 0.60.

**(b) A bench clock, smuggled in with it.** `calib_bundle_r06` derives from
`calib_bundle_lp2_t0p`, so it ships `t0_abs` + `t0_prior_sigma = 5`. That is an
*absolute* arrival time in the readout window, calibrated on the bench against
**its** scintillator trigger and DAQ latency — a property of the trigger, not
of the chamber. run_145 is triggered by n_TOF. At σ = 5 ns it is a pin, not a
nudge.

Measured on tag 004, the same 3,267 events reconstructed both ways:

| | fitted t0, median (p16–p84) |
|---|---|
| no prior | 30 ns (−110 … +450) — free, and poorly determined on 20 samples |
| bench prior on | 236 ns (144 … 298) — sitting on the bench per-bin table (222–305) |

Whatever run_145's true t0 is, it is not the bench's stated to ±5 ns. Left in,
it would have been read as part of the kernel change. `wft_beam.make_bundle`
now drops both when seeding a beam bundle and records what it dropped
(`--keep-t0-prior` for when an in-situ t0 exists); the condor job re-asserts it
after seeding. Commit `11ce347`. **run_79's redo inherits this.**

## 2 · What the kernel alone does

tag 004 (3,267 events, 1/7 of sub-run 0000), arm A, one variable — same events,
same code, same v_drift (42.6), t0 prior off on both sides:

| | lp2 (superseded) | r06 (corrected) | |
|---|---|---|---|
| median tan θ ratio, X | — | — | **×1.0048** |
| median tan θ ratio, Y | — | — | **×1.0382** |
| `slope_reliable` fraction, X / Y | 0.535 / 0.587 | 0.535 / 0.593 | flat |
| k_track median | 1.206 | 1.215 | +0.8 % |
| v_insitu = 42.6/k | 35.33 | 35.06 µm/ns | −0.8 % |
| k_phys (coincident) | 1.248 | 1.251 | +0.2 % |
| r_core at focus | 10.77 | 10.55 mm | −2.0 % |
| frac in capsule | 0.0472 | 0.0475 | flat |
| confirmed / predictable | 513/1047 = 49.0 % | 503/1034 = 48.6 % | flat |

The kernel steepens Y about **eight times more than X**, which is the expected
shape — Y is the plane that shares more. But the imaging is a *fit*: `k`
absorbs nearly all of it, the focus comes out marginally tighter rather than
looser, and the external confirmation rate — the one handle that never touches
the fit's angle scale — does not move.

## 3 · The full re-run

60 condor jobs: arms A, B, D on `calib_bundle_r06` and arm C on
`calib_bundle_lp`, across both sub-runs (`stat090_0000`, 7 file tags;
`stat090_0001`, 8). **Arm C is the control** — its kernel did not change, so
whatever moves on C is the execution path, not the correction.

**The seeding reproduces exactly.** Merged event counts, against the published
tables: 0000 — A 22,434, B 22,096, C 22,788, D 46,218; 0001 — A 25,112,
B 22,800, C 25,682, D 51,319. Every one identical, through a completely
different execution path (per-tag condor jobs under LCG_105 / python 3.9.12,
versus one local process under 3.12).

### Sub-run 0001 — the clean comparison

Its parked tables were already post-restore generation, so the code is common
to both sides and the only thing that differs is the kernel:

| arm | bundle | k_track | | k_phys | | confirmed |
|---|---|---|---|---|---|---|
| **C** | lp (unchanged) | 1.2495 → 1.2495 | **+0.00 %** | 1.5261 → 1.5261 | **+0.00 %** | 39.2 → 39.2 % |
| A | lp2 → r06 | 1.1935 → 1.1925 | −0.08 % | 1.2456 → 1.2429 | −0.22 % | 49.9 → 50.1 % |
| B | lp → r06 | 0.9059 → 0.9082 | +0.25 % | 1.2317 → 1.2154 | −1.32 % | 17.1 → 16.9 % |
| D | lp → r06 | 1.3909 → 1.3655 | −1.83 % | 1.6505 → 1.6193 | −1.89 % | 28.3 → 28.6 % |

Arm C is unchanged **to four decimal places** — the new pipeline is faithful.
Against that zero, the kernel's effect on the in-situ angle scale is ≤ 2 %,
and on the headline arm it is 0.1 %. Arm D moves most, which is the arm whose
bundle was most inverted (1.75) and whose k is known not to be single-valued.

### Sub-run 0000 — kernel *and* code generation

Its parked tables are the pre-restore generation (`angle_constants: null`,
commit 1292d91), when w0/kw were applied post-hoc by the imaging script rather
than in reco. So here arm C does **not** come out identical, and what it shows
is the generation change alone:

| arm | bundle | k_track | | v_insitu | confirmed |
|---|---|---|---|---|---|
| **C** | lp (unchanged) | 1.2273 → 1.2192 | **−0.66 %** | 34.71 → 34.94 | 39.0 → 38.9 % |
| A | lp2 → r06 | 1.1805 → 1.1773 | −0.28 % | **36.09 → 36.19** | **51.2 → 51.2 %** |
| B | lp → r06 | 1.2506 → 1.2531 | +0.20 % | 34.06 → 34.00 | 24.9 → 24.9 % |
| D | lp → r06 | 0.7636 → 0.7289 | −4.54 % | 55.79 → 58.44 | 28.1 → 28.5 % |

Arm A's shift is *smaller* than the control's, so on the headline arm the
kernel correction is not even the dominant term.

### Against the four checks the handoff set

1. **Spot still inside the bore** — yes, and unmoved: k_opt identical on every
   arm (0.75 / 0.575 / 0.85 / 0.5), arm A r_core 11.09 → 11.19 mm, fraction
   inside r ≤ 10 mm 7.73 → 7.70 %.
2. **k_opt moved by nothing**; k_track by −0.3 %. The published
   `v_insitu = 36.1 µm/ns at k = 1.18` becomes **36.2 at k = 1.18** — one digit
   on the deck and in the note.
3. **Arm agreement** unchanged: 0001's spread across arms narrows slightly
   (9.29 → 8.95), 0000's widens (11.96 → 13.31) entirely through arm D, whose
   k is a mixture of two sides (1.18 / 1.96) and not a single number to begin
   with.
4. **External confirmation flat**: 51.2 → 51.2 % on arm A, and within 0.4 pp
   on every other arm and sub-run. This is the check that does not go through
   the fit's angle scale, and it says the track sample did not change.

### What else had to be rebuilt

The numbers were the easy part. Three things on the page were still drawing
the old reconstruction:

- **The note's eight figures had no generator in the repo.** They were made by
  an uncommitted script on 2026-08-13, so the page could not be regenerated —
  numbers from one reconstruction, pictures from another.
  `ntof_tracking/run145_note_figs.py` is that generator, re-derived from the
  note's own captions and reusing `run145_target_imaging`'s selection verbatim.
  It reproduces the note's stated numbers exactly (k 1.1773, k_phys 1.2317,
  3,746/7,312 confirmed, wall medians +95/+41/−70/−125 mm at 48–68 % inside),
  which is what validates it.
- **`run145_wall_segment_3d.py` hard-coded `K_INSITU`** to the previous
  reconstruction's k_phys. It now reads them from the imaging summary.
- **Three deck assets were hand-copied** from the analysis directory with
  nothing in the repo recording it — and PNGs under `slides/assets/img/` are
  gitignored, so git could not report them stale. Identified by md5 against
  the parked originals; `mpgd26/sync_run145_assets.py` now copies them and
  writes a tracked manifest, with `--check` to fail on staleness.

## 4 · How it was run

The laptop that owns this analysis measured ~310 kB/s aggregate to CERN on
2026-08-19 (parallel streams do not help; the link is the cap), ~140 kB/s by
scp from lxplus, and **23 kB/s by xrdcp** — so the 4.9 GB of decoded waveforms
was a ~60 h download. The reco went to the data instead:
`ntof_tracking/condor/`, one job per (arm, file tag), fetch to scratch at CERN,
same `wft_beam` driver, tar back the ~1 MB parquet; `merge_beam_tags.py`
stitches the tags into the one `events_prelim.parquet` per arm that the local
chain expects.

Guards, because each of these is silent once it reaches a worker: the package
refuses a bundle with c₂ > c₁ or one stamped `w0_kw_stale`; the job refuses a
seeded bundle that still carries a t0 prior; the merge refuses to stitch tags
reconstructed with different bundles, or to overwrite a table that is not
already parked.

**Trap, hit once:** `make_beam_package.py` failing on a dirty tree does not
stop a following `rsync && condor_submit` in the same shell — 45 jobs ran on
the previous `code.tar.gz`. Check that `CODE_COMMIT.txt` on lxplus matches HEAD
before believing a result.
